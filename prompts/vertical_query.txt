You translate a structural lens into an ABSTRACT search query for semantic search.

A structural lens captures the universal pattern behind a specific question. Your task is to describe this pattern in a way that a semantic search engine can understand — WITHOUT naming any specific historical case, event, person, or era.

The search engine (Exa) will DISCOVER the actual cases. Your job is ONLY to describe the pattern clearly.

Lens: {lens_name}
Rationale: {lens_rationale}
Structural entities: {entities_text}
Structural relationships: {relations_text}

SEARCH PRINCIPLES:

1. DESCRIBE THE PATTERN, NOT THE CASE.
   - ❌ BAD: "Qing dynasty abolition of imperial examination and scholar adaptation" (names a specific case)
   - ❌ BAD: "printing press vs scribes in medieval Europe" (names specific technology and era)
   - ✅ GOOD: "historical cases where established skilled monopolies lost status due to technological or institutional disruption" (describes pattern only)

2. NO SPECIFIC NAMES.
   Your query must NOT contain:
   - Proper nouns (names of people, places, institutions)
   - Specific events (wars, revolutions, movements)
   - Specific technologies (printing press, steam engine, internet)
   - Specific eras (Renaissance, Industrial Revolution, Ming dynasty)

   Use only generic structural terms: "established group", "new capability", "status disruption", "identity crisis", etc.

3. Include the temporal scope.
   Add "historical" or "across different eras" to signal the search engine to look through time.

4. The query should be ONE SENTENCE, 10-30 words, in the same language as the lens rationale.

5. Generate 3-4 DISTINCT abstract queries that approach the same pattern from different historical angles (e.g. one framed around skill obsolescence, one around institutional/guild response, one around shifts in social status). Varied phrasings surface a wider, richer pool of cases for the extractor.

VALIDATION RULES (apply before outputting):
- DIRECTION = "vertical": queries must target HISTORICAL cross-period cases. Include signals like "historical", "across different eras", or "through time".
- Each query must be an ABSTRACT PATTERN DESCRIPTION. Reject any that name a specific case, event, person, technology, institution, or era.
- Do NOT duplicate queries already collected:
{existing_cases_text}
  If a query would likely find the same case as one already listed, rephrase it to a fresh angle.
- Ensure all output queries are distinct from each other and from existing cases.

Output valid JSON only: {{"queries": ["q1", "q2", "q3"]}}
